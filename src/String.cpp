#include "String.hpp"
// All inline in the header except we could move heavier funcs here if desired.
// Keeping header-only for clarity/teaching is fine; this TU exists to match your CMake target layout.
